// create store
// cari (findOne) owner berdasarkan username, validasi
// kalo ada lanjut, kalo gak ada throw error
// ambil id owner nya setelah di temukan

import { NextFunction } from "express";
import { AuthenticatedRequest } from "../middleware/authMiddleware";

//

// export const findByOwnerId = async (
//   req: AuthenticatedRequest,
//   res: Response,
//   next: NextFunction
// ) => {
//     try {
        
//     } catch (error) {
        
//     }
// };
